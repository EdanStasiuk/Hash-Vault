import Subheader from "../../Subheader";

/**
 * Renders Receive page information for dashboard.
 *
 * @returns {JSX.Element} - Receive page component.
 */
export default function Receive(): JSX.Element {
  return (
    <Subheader label="Receive"/>
  );
}
