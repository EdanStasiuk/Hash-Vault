import Subheader from "../../Subheader";

/**
 * Renders Settings page information for dashboard.
 *
 * @returns {JSX.Element} - Settings page component.
 */
export default function Settings(): JSX.Element {
  return (
    <Subheader label="Settings"/>
  );
}
